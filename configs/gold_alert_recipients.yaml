# Gold Dip Alert — Recipient List
# ----------------------------------
# Every entry below receives every alert this system fires.
# To add someone: append another entry under `recipients:`.
#
# Currency options (two mutually-exclusive modes):
#   currency:   INR              # single currency — body prices localized
#   currencies: [USD, EUR, INR]  # multiple — ONE email with a comparison table
#
# Supported currency codes:
#   USD EUR GBP INR JPY AUD CAD CHF CNY AED SAR SGD HKD NZD ZAR BRL MXN TRY
#
# Optional per-recipient `tiers` filter:
#   seasonal, deadline, opportunistic, major, predicted
# Omit `tiers` to receive all five.

recipients:
  # Owner — one email with a table showing prices in every currency
  - name: Eeshan Bhanap
    email: eeshan1012@gmail.com
    currencies:
      - USD
      - EUR
      - GBP
      - INR
      - JPY
      - AUD
      - CAD
      - CHF
      - CNY
      - AED
      - SAR
      - SGD
      - HKD
      - NZD
      - ZAR
      - BRL
      - MXN
      - TRY
