name: Daily Market Regime Forecast

on:
  schedule:
    # Run at 6 AM EST daily (11 AM UTC)
    - cron: '0 11 * * *'
  workflow_dispatch:  # Allow manual trigger from Actions tab

jobs:
  forecast:
    runs-on: ubuntu-latest
    timeout-minutes: 45  # Increased for full inference pipeline
    permissions:
      contents: write  # Allow git push

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Download previous models from artifacts
        uses: dawidd6/action-download-artifact@v3
        continue-on-error: true
        with:
          workflow: daily-forecast.yml
          name: trained-models
          path: .
          if_no_artifact_found: warn

      - name: Verify downloaded models
        run: |
          echo "Checking for downloaded models..."
          if [ -d "outputs/models" ]; then
            echo "✓ Models directory found"
            ls -lh outputs/models/ || echo "Models directory empty"
          else
            echo "⚠️ No models directory (will train from scratch)"
          fi
          if [ -d "outputs/clustering" ]; then
            echo "✓ Clustering directory found"
            ls -lh outputs/clustering/ || echo "Clustering directory empty"
          else
            echo "⚠️ No clustering directory"
          fi

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements_github_actions.txt

      - name: Verify critical packages
        run: |
          echo "Verifying critical package installation..."
          python -c "import google.cloud.bigquery; print('✓ google-cloud-bigquery installed')"
          python -c "import db_dtypes; print('✓ db-dtypes installed')"
          python -c "import pandas; print('✓ pandas installed')"
          echo "✓ All critical packages verified"

      - name: Create credentials file from secret
        run: |
          echo '${{ secrets.GCP_CREDENTIALS }}' > /tmp/gcp_credentials.json
          echo "GOOGLE_APPLICATION_CREDENTIALS=/tmp/gcp_credentials.json" >> $GITHUB_ENV

      - name: Run daily forecast pipeline
        env:
          PYTHONUNBUFFERED: 1
        run: |
          echo "Starting daily market regime forecast..."
          python run_daily_update.py

      - name: Upload trained models
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: trained-models
          path: |
            outputs/models/*.joblib
            outputs/clustering/*.parquet
            outputs/forecasting/models/*/nf_bundle_v*/**
          retention-days: 90

      - name: Log daily predictions
        run: |
          # Ensure db-dtypes is available before logging
          python -c "import db_dtypes" || pip install db-dtypes==1.3.1
          python log_daily_predictions.py --output DAILY_PREDICTIONS.md

      - name: Commit and push results
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "GitHub Actions Bot"

          # Add daily predictions file
          git add DAILY_PREDICTIONS.md || true

          # Commit if there are changes
          if git diff --staged --quiet; then
            echo "No changes to commit"
          else
            git commit -m "🤖 Daily forecast update $(date +'%Y-%m-%d %H:%M UTC')"
            git push
            echo "✅ Forecast results committed and pushed"
          fi

      - name: Keep Streamlit dashboard awake
        if: success()
        run: |
          echo "Pinging Streamlit dashboard to prevent sleep..."
          curl -I https://marketpulsedashboard.streamlit.app || true
          echo "✓ Dashboard pinged successfully"

      - name: Upload forecast artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: forecast-${{ github.run_number }}
          path: |
            DAILY_PREDICTIONS.md
          retention-days: 7

      - name: Summary
        if: success()
        run: |
          echo "✅ Daily forecast completed successfully!"
          echo "📅 Timestamp: $(date +'%Y-%m-%d %H:%M UTC')"
          echo "🔗 View results: https://github.com/${{ github.repository }}/blob/main/DAILY_PREDICTIONS.md"
          echo "📊 Dashboard: https://marketpulsedashboard.streamlit.app"
