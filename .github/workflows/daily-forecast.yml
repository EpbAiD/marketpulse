name: Daily Market Regime Forecast

on:
  schedule:
    # Run at 6 AM EST daily (11 AM UTC)
    - cron: '0 11 * * *'
  workflow_dispatch:  # Allow manual trigger from Actions tab

jobs:
  forecast:
    runs-on: ubuntu-latest
    timeout-minutes: 45  # Increased for full inference pipeline
    permissions:
      contents: write  # Allow git push

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Install Playwright for screenshots
        run: |
          pip install playwright
          playwright install chromium
          playwright install-deps

      - name: Create credentials file from secret
        run: |
          echo '${{ secrets.GCP_CREDENTIALS }}' > /tmp/gcp_credentials.json
          echo "GOOGLE_APPLICATION_CREDENTIALS=/tmp/gcp_credentials.json" >> $GITHUB_ENV

      - name: Start Streamlit dashboard in background
        run: |
          nohup streamlit run dashboard/app.py --server.port 8501 --server.headless true > streamlit.log 2>&1 &
          echo "Waiting for Streamlit to start..."
          sleep 15
          curl -f http://localhost:8501 || (cat streamlit.log && exit 1)
          echo "✓ Streamlit server running"

      - name: Run daily forecast pipeline
        env:
          PYTHONUNBUFFERED: 1
        run: |
          echo "Starting daily market regime forecast..."
          python run_daily_update.py

      - name: Log daily predictions
        run: |
          python log_daily_predictions.py --output DAILY_PREDICTIONS.md

      - name: Capture dashboard screenshot
        run: |
          python capture_dashboard_screenshot.py --url http://localhost:8501

      - name: Stop Streamlit server
        if: always()
        run: |
          pkill -f streamlit || true

      - name: Commit and push results
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "GitHub Actions Bot"

          # Add only the files that should be updated daily
          git add DAILY_PREDICTIONS.md || true
          git add assets/dashboard.png || true

          # Commit if there are changes
          if git diff --staged --quiet; then
            echo "No changes to commit"
          else
            git commit -m "🤖 Daily forecast update $(date +'%Y-%m-%d %H:%M UTC')"
            git push
            echo "✅ Forecast results committed and pushed"
          fi

      - name: Upload forecast artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: forecast-${{ github.run_number }}
          path: |
            DAILY_PREDICTIONS.md
            assets/dashboard.png
          retention-days: 7

      - name: Summary
        if: success()
        run: |
          echo "✅ Daily forecast completed successfully!"
          echo "📅 Timestamp: $(date +'%Y-%m-%d %H:%M UTC')"
          echo "🔗 View results: https://github.com/${{ github.repository }}/blob/main/DAILY_PREDICTIONS.md"
