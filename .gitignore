# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual Environment
venv/
ENV/
env/
.venv

# IDE
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# Jupyter Notebook
.ipynb_checkpoints

# Environment variables
.env
.env.local

# =============================================================================
# PROJECT OUTPUTS - Selective tracking
# =============================================================================
# Ignore most outputs but ALLOW trained models to be committed

# Ignore temporary/generated outputs
outputs/fetched/
outputs/engineered/
outputs/selected/
outputs/diagnostics/
lightning_logs/

# ALLOW these model directories to be tracked:
# - outputs/models/ (HMM, classifier)
# - outputs/clustering/ (cluster assignments)
# - outputs/forecasting/models/ (neural network models)
# These are NOT ignored!

# =============================================================================
# JSON files - selective
# =============================================================================
# Ignore most JSON but allow specific patterns
*.json
!configs/*.json
!outputs/forecasting/models/*_versions.json

# Log files
*.log
*.jsonl
!logs/.gitkeep

# Screenshots - only track latest in assets/
dashboard_screenshots/
!assets/

# BigQuery credentials (NEVER commit these!)
regime01-*.json
*credentials*.json

# Temporary files
*.tmp
*.bak
*.cache

# =============================================================================
# MODEL FILES - ALLOW for persistence
# =============================================================================
# We WANT to track trained models in the repo for persistence
# DO NOT ignore: .joblib, .pkl in outputs/models/
# DO NOT ignore: nf_bundle directories in outputs/forecasting/models/

# Only ignore model files OUTSIDE of outputs/
/models/
*.h5
*.pt
*.pth

# =============================================================================
# DATA FILES - Ignore raw data (too large)
# =============================================================================
*.csv
*.tsv
data/
INTERVIEW_PREP.md

# Parquet files - ignore except cluster assignments
*.parquet
!outputs/clustering/*.parquet
