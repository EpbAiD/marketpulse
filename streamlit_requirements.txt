# Streamlit Cloud Requirements
# Lighter version without heavy ML dependencies (torch, langgraph, etc.)
# The dashboard only displays data from BigQuery, doesn't train models

# Dashboard visualization
streamlit==1.51.0
plotly==6.3.1

# Data processing
pandas==2.2.3
numpy==1.26.4
pyarrow==17.0.0

# Cloud storage (BigQuery)
google-cloud-bigquery==3.38.0
google-cloud-bigquery-storage==2.35.0
google-cloud-core==2.5.0
db-dtypes==1.3.1

# Utilities
joblib==1.4.2
pyyaml==6.0.2
matplotlib==3.9.4
